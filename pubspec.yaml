name: KandaviaVIP
description: A Flutter project to render HTML files.

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"  # Asegúrate de que esta versión sea compatible con tus dependencias

dependencies:
  flutter:
    sdk: flutter
  flutter_html: ^2.2.1
  flutter_layout_grid: ^1.0.6  # Cambia a la versión más reciente disponible
  flutter_math_fork: ^0.5.0  # O la versión que necesites
  win32: ^3.1.4  # Especifica la versión para garantizar compatibilidad

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/html/mi_carnet_vip_canjear.html  # Asegúrate de que el archivo HTML existe en esta ruta
